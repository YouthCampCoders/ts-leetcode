/**
 * 输入数字 n，按顺序打印出从 1 到最大的 n 位十进制数。比如输入 3，则打印出 1、2、3 一直到最大的 3 位数 999。
 */

/**
 * 考虑大数问题，以 n = 100 为例，那么最终输出的结果将来到10^100级，远超 js(+-2^53) 以及大多主流引擎所能容下的数字类型长度
 */

function dfs(n: number) {}

function printNumbers(n: number): string[] {
  const res: string[] = []
  return res
}

printNumbers(100)

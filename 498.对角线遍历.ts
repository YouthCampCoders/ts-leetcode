/*
 * @lc app=leetcode.cn id=498 lang=typescript
 *
 * [498] 对角线遍历
 */

// @lc code=start
function findDiagonalOrder(mat: number[][]): number[] {
  const res: number[] = []
  let flag = true // 为true时候向右上角，为false时向左下角
  let x = 0,
    y = 0
  const maxSum = mat.length + mat[0].length - 2

  return res
}
// @lc code=end
